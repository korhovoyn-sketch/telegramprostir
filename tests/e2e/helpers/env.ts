import { existsSync } from 'node:fs'

/**
 * Chromium, предвстановлений у пісочниці Claude Code на вебі. `playwright install`
 * там заборонений, тож ця збірка ЗАМОРОЖЕНА і не збігається з тією, що її ставить
 * CI: заміряно **141.0.7390.37** тут проти **148.0.7778.96** на раннері.
 */
export const SANDBOX_CHROMIUM = '/opt/pw-browsers/chromium-1194/chrome-linux/chrome'

/**
 * Чи біжимо в пісочниці. Важливо не лише для `executablePath`, а й для візуального
 * бейслайна: сім мажорів різниці в Chromium дають іншу растеризацію тексту, тобто
 * кадр, знятий тут, на раннері виглядає як «дизайн поїхав» (заміряно: ratio 0.02
 * при порозі 0.01, БЕЗ жодного зсуву геометрії). Бейслайни іменуються по
 * ПЛАТФОРМІ (`…-iphone-se-linux.png`), а не по збірці браузера, тож сама
 * розбіжність невидима, доки не побіжить CI.
 */
export const IN_SANDBOX = existsSync(SANDBOX_CHROMIUM)
